# Web (Next.js) — public env vars exposed to the browser must be prefixed NEXT_PUBLIC_
# Copy to apps/web/.env.local and adjust as needed.

# Set to 'true' after configuring GOOGLE_CLIENT_ID/SECRET on the API
NEXT_PUBLIC_GOOGLE_AUTH_ENABLED=false
