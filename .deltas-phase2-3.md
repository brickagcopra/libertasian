# Phase 2 + 3 Deltas — features dropped in the redesign

> **2026-05-11 smoke verification update — 5 surfaces verified on emulator with seeded data; DD1-DD4 retracted as ❌ FALSE FLAG.**
>
> The 2026-05-10 code-grep audit below was correct for RD4 / DD5 / DD6 / DD7-DD10 / T4-T6 and the inline ✅ marks held up under emulator smoke. But it was wrong about DD1-DD4: those four "renderer" branches (IRAC / MCQ / Essay / Subject outline) were unreachable dead code — there is no DigestType enum value, no Prisma schema field, and no seeded row that would ever exercise them. Ground truth: digests in this codebase are 5-field case-digests (Facts / Issues / Ruling / Doctrine / Dispositive); MCQ / Essay / Outline ship via `DerivativeArtifact` + `/derivatives` route + the Library tab; IRAC has no schema or rows. The dead `digest-renderers.tsx` + IRAC/MCQ/Essay/Outline switches in `digest/[id].tsx` were removed in commit `9141109` ("chore(mobile): cut DD1-DD4 dead code").
>
> Mapping bug surfaced by smoke: `digest/[id].tsx` referenced phantom enum values (`official_source`, `community`, `user_text`) that do not match the actual API enum (`official_pipeline`, `admin_generated`, `user_scan`, `user_upload`, `camera_capture`, per `apps/api/src/modules/digests/dto/create-digest.dto.ts:26`). Caused DD5 disclaimer to never render and DD10 source-origin badge to render with wrong label/tone on the seeded Agabon row. Fixed in `digest/[id].tsx:32-40,93-99,112-126` — confirmed live.
>
> | ID | Surface | Verified | Screenshot |
> |---|---|---|---|
> | RD4 | ContentDisclaimer on case reader | ✅ | `.smoke-deltas-29-rd4-case.png` |
> | RD4 | ContentDisclaimer on statute reader | ✅ | `.smoke-deltas-28-rd4-statute.png` |
> | DD5 | ContentDisclaimer on Agabon case digest (`'official_pipeline' && 'approved'` → `official_text` class) | ✅ | `.smoke-deltas-45-agabon-fixed.png` |
> | DD6 | ExportButton in digest footer | ✅ | `.smoke-deltas-25-agabon.png` (visible in footer row) |
> | DD7 | Confidence 95% badge on Agabon | ✅ | `.smoke-deltas-45-agabon-fixed.png` |
> | DD8 | Review status APPROVED badge | ✅ | `.smoke-deltas-45-agabon-fixed.png` |
> | DD9 | Visibility PUBLIC EDITORIAL badge | ✅ | `.smoke-deltas-45-agabon-fixed.png` |
> | DD10 | Source-origin OFFICIAL SOURCE badge (success tone, mapped from `official_pipeline`) | ✅ | `.smoke-deltas-45-agabon-fixed.png` |
> | T4 | Search → AI Summary sub-tab | ✅ | `.smoke-deltas-32-t4-ai-summary.png` |
> | T5/T6 | Search → Digests sub-tab | ✅ | `.smoke-deltas-33-t5-digests.png` |
> | DD1-DD4 | IRAC / MCQ / Essay / Outline renderers | ❌ FALSE FLAG | n/a — dead code, no schema, no rows |
>
> Current wiring (post-cut, post-fix):
>
> | ID | Wired at |
> |---|---|
> | RD4 | `apps/mobile/src/app/reader/[id].tsx:35,272-275` (`disclaimerSlot`) |
> | DD5 | `apps/mobile/src/app/digest/[id].tsx:10,112-126,189-190` (`disclaimerClassFor()` → `ContentDisclaimer` in `disclaimerSlot`) |
> | DD6 | `apps/mobile/src/app/digest/[id].tsx:11,205-211` (`footerSlot` → `ExportButton`) |
> | DD7-DD10 | `apps/mobile/src/app/digest/[id].tsx:69-110` (`buildBadges` → review-status / visibility / source-origin / confidence-%) |
> | T4-T6 | `apps/mobile/src/app/(tabs)/search.tsx:14-16,146-161` (`tabBarSlot` + `customResults` for AI Summary / Digests) |

Audit of every legacy → wrapper rewrite across the 8 modified routes (commits `a9d2e35` Phase 2, `62afda1` Phase 3). For each item, **decide: restore in this PR (with a design-aware control) or accept the drop and ship.**

Comparison baseline: `89515c9` (pre-Phase-2). Source: `git show 89515c9:<path>`.

Legend:
- 🔴 **Functional / data-mutation**: writes, money, compliance, navigation primitives
- 🟡 **Information surface**: badges, metadata, status, disclaimers — affects user trust/decisions
- 🔵 **Convenience UX**: shortcuts, history, recently-viewed — discoverability layer
- ✅ **Already restored** in `4cae2e1` (verified by code-grep 2026-05-10; smoke pending)

---

## 1. `(auth)/login.tsx`

| # | Feature | Severity | Notes |
|---|---|---|---|
| L1 | **Client-side validation** ("Email is required", "Enter a valid email", "Password is required") | 🟡 | New screen submits raw — relies entirely on API errors. Worse UX (network round-trip per typo) but parallels what the redesign shows. |
| L2 | **MFA modal** | preserved | Re-implemented as a sheet titled "Two-step verification" — kept. |
| L3 | **`Login Failed` Alert title** → inline `Invalid email or password.` text | preserved-but-changed | Shape change, not a drop. |

**Question:** Restore inline validation messages (L1)? Design has `<Input error="...">` slot already wired.

---

## 2. `(auth)/register.tsx`

| # | Feature | Severity | Notes |
|---|---|---|---|
| R1 | (none meaningful — the new register kept all 6 validation rules and 4 error branches) | — | Net rewrite preserved behavior. |

**Nothing to decide here.**

---

## 3. `(onboarding)/index.tsx`

| # | Feature | Severity | Notes |
|---|---|---|---|
| O1 | (none — same 5-step flow, same role list, same subjects/areas, same skip + complete) | — | Faithful rewrite. |

**Nothing to decide here.**

---

## 4. `documents/index.tsx`

| # | Feature | Severity | Notes |
|---|---|---|---|
| D1 | **Court filter** (`SUPREME_COURT` / `COURT_OF_APPEALS` / `SANDIGANBAYAN` / `CTA`) | 🔴 | Legacy filter panel had a `Court` chip row. New screen has no court filter. |
| D2 | **Bar Subject filter** (loaded via `useBarSubjects`) | 🔴 | Legacy had a `Bar Subject` chip row from API. New screen drops it. |
| D3 | **Active filter count badge** on the filter toggle | 🟡 | Legacy showed `(3)` on the filter button when 3 filters active. New design has a filter icon but no count badge. |
| D4 | **Search submit + clear button inside the search bar** | 🔵 | Legacy: typing a query + pressing Enter or the X button worked. New screen's search bar is a Pressable that opens a search modal (which doesn't exist yet — currently a no-op stub). |
| D5 | **Pull-to-refresh** | 🔵 | Legacy had `RefreshControl`. New screen doesn't. |
| D6 | **Infinite scroll for next page** | 🔴 | Legacy auto-loaded next page on scroll-end. New screen has a "Load more" pill instead — works, but a different UX. |

**Question:** D1, D2 are real product filters that real users use to narrow Philippine legal corpus searches. Restoring them as a "More filters" sheet/modal? Or are court+subject filters intentionally dropped from the redesigned IA?

---

## 5. `settings/index.tsx`

| # | Feature | Severity | Notes |
|---|---|---|---|
| S1 | (none — drawer-replacement rows preserved Digests/Study/Feed/Workspace/Subscription/Plans/Usage/API Keys/Admin/Notifications/Sign out) | — | All 10 entries kept. |

**Nothing to decide here.** (Caveat: theme-card tap doesn't visibly switch theme — separate bug from your note 4, will fix in smoke-fix pass regardless.)

---

## 6. `(tabs)/index.tsx` (was Search; now Home)

This is the biggest semantic change. Legacy was a 937-line full Search experience. We replaced it with a 33-line Home wrapper and put Search at a new `/(tabs)/search`. Everything below was specific to the old Search screen.

| # | Feature | Severity | Notes |
|---|---|---|---|
| T1 | **Recent Searches** section with `Clear` action | 🔵 | Legacy `useSearchHistory()` is still wired in the codebase but no UI shows it. New SearchScreen has no history surface. |
| T2 | **Recently Viewed** documents section on the empty state | 🔵 | Legacy `useRecentlyViewed()` → grid of recent docs as quick re-entry points. Still tracked in storage, no UI shows it. |
| T3 | **Filter panel on Search** with: documentType, court, GR no., ponente, dateFrom, dateTo, barSubject (7 filters) | 🔴 | New SearchScreen has 5 cosmetic chips (`All/Cases/Articles/Statutes/Outlines`); only `Cases` and `Statutes` actually map to a documentType. Power-user filters all gone. |
| T4 | **`SearchTabBar` with sub-tabs**: Documents / AI Summary / Digests | ✅ verified | `(tabs)/search.tsx:14,146-153`. Smoke `.smoke-deltas-32-t4-ai-summary.png` shows AI Summary sub-tab active. |
| T5 | **AI Summary streaming results** (`AiSummaryResults`) | ✅ verified | `(tabs)/search.tsx:15,157-158`. Smoke `.smoke-deltas-32-t4-ai-summary.png`. |
| T6 | **Digests results tab** (`DigestsResults`) | ✅ verified | `(tabs)/search.tsx:16,159-160`. Smoke `.smoke-deltas-33-t5-digests.png`. |
| T7 | **"Generate Digest" inline button** on each search result card | 🟡 | Legacy let you generate a digest directly from a search hit. New SearchScreen results don't expose that affordance. |

**Question:** T3-T7 are core RAG features. Restoring AI summary + digest sub-tabs is a non-trivial design problem (where do they go in the new SearchScreen layout?). Want me to: (a) keep them as-is and accept the drop, (b) restore the SearchTabBar above results, (c) move AI Summary to a dedicated route?

---

## 7. `reader/[id].tsx` (Document reader — your specific concern)

| # | Feature | Severity | Notes |
|---|---|---|---|
| RD1 | **Citations tab** (`useDocumentCitations` + `DocumentCitation` cards) | 🔴 | "What this case cites" — core legal-research feature. Gone. |
| RD2 | **Related documents tab** (`useRelatedDocuments` + relevance score) | 🔴 | "Cases that cite this / are similar." Gone. |
| RD3 | **Tab switcher** (Sections / Citations / Related) | 🔴 | The whole tabbed surface is gone — new design has only sections. |
| RD4 | **`ContentDisclaimer`** banner | ✅ verified | `reader/[id].tsx:35,68-74,272-275`. Smoke `.smoke-deltas-29-rd4-case.png` (case) and `.smoke-deltas-28-rd4-statute.png` (statute) — disclaimer banner visible above the reader content. |
| RD5 | **`OfflineBadge` + Save Offline** action (`useOfflineCodals`) | 🟡 | Legacy let users save codals for offline reading. The hook still exists. |
| RD6 | **"View Existing Digest" button** when `useDigests({legalDocumentId})` returns one | 🟡 | If user already generated a digest for this doc, legacy showed a one-tap link. Gone. The new wrapper's `onAdd` always proposes generating a NEW one even if one exists. |
| RD7 | **Generate Digest with quota dialog** | preserved | Wired to the FAB `+`. |
| RD8 | **Bookmark with optional note** (`bookmarkNote` TextInput in inline form) | 🟡 | Legacy let you attach a note when bookmarking. New just creates an unannotated bookmark. |
| RD9 | **`Stack.Screen` title** and back button via stack | preserved-but-changed | Replaced by the in-screen back chevron in the design. Probably fine. |
| RD10 | **Section page-range badges** (`p.1-3`) | 🔵 | Legacy showed `pageStart-pageEnd` per section. New screen renders heading + paragraphs only, no page span. |

**Particular concern from your note:** RD4 (ContentDisclaimer) is a **compliance** surface, not just nice-to-have. CLAUDE.md domain rules require provenance/source-trust signaling. **Strong recommendation: restore RD4 unconditionally.** The other items are product judgment.

---

## 8. `digest/[id].tsx` (Digest detail / Article reader)

This is the second-biggest gap. The new `DigestDetailScreen` was designed for **case-digest** shape only (Facts/Issues/Ruling/Doctrine/Dispositive). The legacy reader rendered **five distinct digest types** with bespoke UIs.

| # | Feature | Severity | Notes |
|---|---|---|---|
| DD1 | **IRAC digest renderer** (`iracIssue` / `iracRule` / `iracApplication` / `iracConclusion`) | ❌ FALSE FLAG | No `'irac'` value in `DigestType` enum (`apps/api/src/modules/digests/dto/generate-digest.dto.ts:14`); no Prisma columns; no seeded rows. The renderer in `digest-renderers.tsx` was dead code. Removed in `9141109`. |
| DD2 | **MCQ practice digest renderer** (`mcqStem` + 4 choices + reveal + correct/wrong indicator + `mcqExplanation`) | ❌ FALSE FLAG | MCQ ships via `DerivativeArtifact` + `/derivatives/[id]` route + Library tab — never via the digest reader. No `mcq*` columns on `Digest`. Removed in `9141109`. |
| DD3 | **Essay digest renderer** (`essayPrompt` + `essayModelAnswer`) | ❌ FALSE FLAG | Essay ships via `DerivativeArtifact`, same as MCQ. No `essay*` columns on `Digest`. Removed in `9141109`. |
| DD4 | **Subject outline renderer** (`subjectOutlineJson` recursive tree) | ❌ FALSE FLAG | Outlines ship via `DerivativeArtifact` + Library tab. No `subjectOutlineJson` column on `Digest`. Removed in `9141109`. |
| DD5 | **`ContentDisclaimer`** | ✅ verified | `digest/[id].tsx:10,112-126,189-190`. Smoke 2026-05-11 on Agabon (`.smoke-deltas-45-agabon-fixed.png`) — disclaimer reads "This is an official text sourced from a Philippine government publication." after `'official_pipeline' && 'approved'` mapping fix. |
| DD6 | **`ExportButton`** (export digest as PDF/Markdown) | ✅ verified | `digest/[id].tsx:11,205-211`. Smoke `.smoke-deltas-25-agabon.png` shows the export icon in the digest footer row. (Free-vs-paid gating handled inside `ExportSheet`.) |
| DD7 | **Confidence score display** (e.g. "92%" with color-graded threshold) | ✅ verified | `digest/[id].tsx:101-107` (in `buildBadges`). Smoke shows "CONFIDENCE 95%" badge on Agabon (success tone, ≥0.85). |
| DD8 | **Review status badge** ("draft" / "ai_generated" / "needs_human_review" / "approved") | ✅ verified | `digest/[id].tsx:72-82` emits label-mapped badge. Smoke shows "APPROVED" badge in success tone on Agabon. |
| DD9 | **Visibility badge** ("private" / "public_editorial") | ✅ verified | `digest/[id].tsx:84-91` emits visibility badge. Smoke shows "PUBLIC EDITORIAL" badge on Agabon (info tone). |
| DD10 | **Source origin badge** | ✅ verified | `digest/[id].tsx:32-40,93-99` — API enum-aligned `SOURCE_LABELS` (`official_pipeline` → `Official source`, etc.) + success tone for `official_pipeline`. Smoke shows "OFFICIAL SOURCE" badge in success tone on Agabon (was rendering "official pipeline" with info tone before the mapping fix). |
| DD11 | **Created / updated timestamps** | 🔵 | Metadata row at top. Gone. |
| DD12 | **"View Source Document" button** | preserved-as-CTA | Now wired to the StickyCTA. |

**Retracted (2026-05-11):** DD1-DD4 (IRAC/MCQ/Essay/Outline renderers) were a false flag — see top-of-doc banner. Ground truth: digests in this codebase are 5-field case-digests; MCQ/Essay/Outline ship via `DerivativeArtifact` + Library + `/derivatives`; IRAC has no schema or rows. The dead `digest-renderers.tsx` and IRAC/MCQ/Essay/Outline branches in `digest/[id].tsx` were removed in `9141109`.

DD5-DD10 are compliance/trust surfaces and the CLAUDE.md provenance rule from RD4 applies — all five surfaces are now wired AND verified on emulator (see top-of-doc table).

DD6 (ExportButton) is a paid-tier feature — wired in the digest footer; free-vs-paid gating happens inside `ExportSheet`.

---

## Summary table — items with strong restoration recommendation

**All real surfaces verified on emulator 2026-05-11 with seeded data (see top-of-doc smoke table for screenshot links). DD1-DD4 retracted as ❌ FALSE FLAG.**

| ID | What | Why it can't be a silent drop | Status |
|---|---|---|---|
| **RD4** | ContentDisclaimer on document reader | CLAUDE.md compliance rule (provenance surfacing) | ✅ verified |
| **DD1** | IRAC digest renderer | ~~~25% of digest types unrenderable~~ | ❌ FALSE FLAG (no enum / no schema / dead code, removed in `9141109`) |
| **DD2** | MCQ practice digest renderer | ~~Whole product feature gone~~ | ❌ FALSE FLAG (ships via `DerivativeArtifact`, removed in `9141109`) |
| **DD3** | Essay digest renderer | ~~Whole product feature gone~~ | ❌ FALSE FLAG (ships via `DerivativeArtifact`, removed in `9141109`) |
| **DD4** | Subject outline renderer | ~~Whole product feature gone~~ | ❌ FALSE FLAG (ships via `DerivativeArtifact`, removed in `9141109`) |
| **DD5** | ContentDisclaimer on digest | CLAUDE.md compliance rule | ✅ verified (after enum-mapping fix) |
| **DD6** | ExportButton | Paid-tier surface | ✅ verified |
| **DD7-10** | Confidence / status / visibility / source-origin badges | Trust calibration for AI output | ✅ verified (DD10 after enum-mapping fix) |
| **T4-T6** | AI Summary + Digests sub-tabs in Search | Core RAG product features | ✅ verified |

## Items with judgment-call recommendation

L1, D1, D2, D3, D5, D6, T1, T2, T3, T7, RD1, RD2, RD3, RD5, RD6, RD8, RD10, DD11.

## Items I'd accept dropping

D4 (search modal stub — re-do in Phase 4), RD9 (stack title), DD12 (already preserved as CTA).

---

## Next step

Strong-rec set is verified on emulator (see top-of-doc smoke table). DD1-DD4 retracted. Only remaining decisions are the **judgment-call** list above (L1, D1, D2, D3, D5, D6, T1, T2, T3, T7, RD1, RD2, RD3, RD5, RD6, RD8, RD10, DD11). Tell me per ID which to restore.
