# ==========================================================================
# LIBERTASIAN — Production Docker Compose
# Per PDD Section 10: VPS-Friendly Phase 1 Deployment
# ==========================================================================
#
# Usage:
#   docker compose -f docker-compose.prod.yml up -d
#
# Requires:
#   - .env file with production secrets
#   - SSL certificates in ./infrastructure/nginx/ssl/
#   - Images pre-built or registry access configured

services:
  # ====================
  # Reverse Proxy
  # ====================
  nginx:
    image: nginx:1.27-alpine
    container_name: libertasian-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infrastructure/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./infrastructure/nginx/ssl:/etc/nginx/ssl:ro
    depends_on:
      api:
        condition: service_healthy
      web:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 256M

  # ====================
  # Web App (Next.js)
  # ====================
  web:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile.web
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://libertasian.com/api/v1}
    container_name: libertasian-web
    restart: always
    expose:
      - "3000"
    env_file: .env
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3000"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 1G

  # ====================
  # NestJS API
  # ====================
  api:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile.api
    container_name: libertasian-api
    restart: always
    expose:
      - "3001"
    env_file: .env
    environment:
      NODE_ENV: production
      APP_PORT: "3001"
      CLAMAV_HOST: clamav
      CLAMAV_PORT: "3310"
      CLAMAV_ENABLED: "true"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      clamav:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:3001/api/v1/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 2G

  # ====================
  # PostgreSQL + pgvector
  # ====================
  postgres:
    image: pgvector/pgvector:pg16
    container_name: libertasian-postgres
    restart: always
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-libertasian}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-libertasian}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-libertasian}"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 4G
    command:
      - "postgres"
      - "-c"
      - "max_connections=200"
      - "-c"
      - "shared_buffers=1GB"
      - "-c"
      - "effective_cache_size=3GB"
      - "-c"
      - "maintenance_work_mem=256MB"
      - "-c"
      - "wal_buffers=16MB"
      - "-c"
      - "random_page_cost=1.1"
      - "-c"
      - "effective_io_concurrency=200"
      - "-c"
      - "autovacuum_vacuum_scale_factor=0.01"
      - "-c"
      - "log_min_duration_statement=1000"

  # ====================
  # Redis
  # ====================
  redis:
    image: redis:7-alpine
    container_name: libertasian-redis
    restart: always
    ports:
      - "127.0.0.1:6379:6379"
    command: >
      redis-server
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
      --appendonly yes
      --appendfsync everysec
      --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 768M

  # ====================
  # OpenSearch
  # ====================
  opensearch:
    image: opensearchproject/opensearch:2.17.0
    container_name: libertasian-opensearch
    restart: always
    ports:
      - "127.0.0.1:9200:9200"
    environment:
      - discovery.type=single-node
      - OPENSEARCH_INITIAL_ADMIN_PASSWORD=${OPENSEARCH_PASSWORD}
      - bootstrap.memory_lock=true
      - "OPENSEARCH_JAVA_OPTS=-Xms1g -Xmx1g"
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65536
        hard: 65536
    volumes:
      - opensearch_data:/usr/share/opensearch/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sk https://localhost:9200 -u admin:${OPENSEARCH_PASSWORD} | grep -q 'opensearch'"]
      interval: 30s
      timeout: 10s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: "4"
          memory: 3G

  # ====================
  # MinIO (Object Storage)
  # ====================
  minio:
    image: minio/minio:latest
    container_name: libertasian-minio
    restart: always
    ports:
      - "127.0.0.1:9000:9000"
      - "127.0.0.1:9001:9001"
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY}
    command: server /data --console-address ":9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 30s
      timeout: 10s
      retries: 5
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 512M

  minio-init:
    image: minio/mc:latest
    container_name: libertasian-minio-init
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set myminio http://minio:9000 $${S3_ACCESS_KEY} $${S3_SECRET_KEY};
      mc mb --ignore-existing myminio/libertasian-uploads;
      mc mb --ignore-existing myminio/libertasian-corpus;
      exit 0;
      "
    env_file: .env

  # ====================
  # ClamAV (Malware Scanning)
  # ====================
  clamav:
    image: clamav/clamav:1.4
    container_name: libertasian-clamav
    restart: always
    expose:
      - "3310"
    volumes:
      - clamav_data:/var/lib/clamav
    healthcheck:
      test: ["CMD", "clamdscan", "--ping", "1"]
      interval: 60s
      timeout: 10s
      retries: 3
      start_period: 120s
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 1G

  # ====================
  # OCR Service (Python)
  # ====================
  ocr-service:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile.ocr
    container_name: libertasian-ocr-service
    restart: always
    expose:
      - "8002"
    environment:
      OCR_TESSERACT_CMD: tesseract
      OCR_TESSERACT_LANG: eng
      OCR_QUALITY_REJECT_THRESHOLD: "0.2"
      OCR_QUALITY_WARN_THRESHOLD: "0.4"
      OCR_MAX_IMAGE_WIDTH: "2048"
      OCR_MAX_IMAGE_HEIGHT: "2048"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 1G

  # ====================
  # RAG Service (Python FastAPI)
  # ====================
  rag-service:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile.rag
    container_name: libertasian-rag-service
    restart: always
    expose:
      - "8000"
    env_file: .env
    environment:
      RAG_OPENSEARCH_URL: https://opensearch:9200
      RAG_REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      RAG_DATABASE_URL: postgresql://${POSTGRES_USER:-libertasian}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-libertasian}
    depends_on:
      opensearch:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2G

  # ====================
  # Worker Service (Celery)
  # ====================
  worker-service:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile.worker
    container_name: libertasian-worker-service
    restart: always
    env_file: .env
    environment:
      WORKER_REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      WORKER_CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD}@redis:6379/1
      WORKER_DATABASE_URL: postgresql://${POSTGRES_USER:-libertasian}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-libertasian}
      WORKER_OCR_SERVICE_URL: http://ocr-service:8002
      WORKER_S3_ENDPOINT: http://minio:9000
      WORKER_S3_ACCESS_KEY: ${S3_ACCESS_KEY}
      WORKER_S3_SECRET_KEY: ${S3_SECRET_KEY}
      WORKER_S3_BUCKET_UPLOADS: libertasian-uploads
      WORKER_S3_BUCKET_CORPUS: libertasian-corpus
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      ocr-service:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2G

  # ====================
  # Worker Beat (Celery Scheduler)
  # ====================
  worker-beat:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile.worker
    container_name: libertasian-worker-beat
    restart: always
    command: ["celery", "-A", "src.celery_app", "beat", "--loglevel=info"]
    env_file: .env
    environment:
      WORKER_REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379/0
      WORKER_CELERY_RESULT_BACKEND: redis://:${REDIS_PASSWORD}@redis:6379/1
      WORKER_DATABASE_URL: postgresql://${POSTGRES_USER:-libertasian}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-libertasian}
      WORKER_S3_ENDPOINT: http://minio:9000
      WORKER_S3_ACCESS_KEY: ${S3_ACCESS_KEY}
      WORKER_S3_SECRET_KEY: ${S3_SECRET_KEY}
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M

  # ====================
  # Embedding Service (Python)
  # ====================
  embedding-service:
    build:
      context: .
      dockerfile: infrastructure/docker/Dockerfile.embedding
    container_name: libertasian-embedding-service
    restart: always
    expose:
      - "8001"
    environment:
      EMBEDDING_MODEL_NAME: ${EMBEDDING_MODEL_NAME:-BAAI/bge-small-en-v1.5}
      EMBEDDING_EMBEDDING_DIM: "${EMBEDDING_DIM:-384}"
      EMBEDDING_DEVICE: ${EMBEDDING_DEVICE:-cpu}
      EMBEDDING_MAX_BATCH_SIZE: "64"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 2G

volumes:
  postgres_data:
  redis_data:
  opensearch_data:
  minio_data:
  clamav_data:
